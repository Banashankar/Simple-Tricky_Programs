##Algorithm for Grouping Anagrams using Streams##
-----------------------------------------------
Step1:-Create stream from the input array of strings.

Step2:Group the strings by a key generated by sorting the characters each string.The Collectors.groupingBy() method.

Step3:Collect the values of the resulting map(list of anagrams) into a list of lists using the Collectors.toList() method.


##Algorithm for Grouping Anagrams
----------------------------------
import java.util.*;

public class AnagramGrouping {
    public List<List<String>> groupAnagrams(String[] strs) {
        Map<String, List<String>> anagramMap = new HashMap<>();

        for (String str : strs) {
            char[] charArray = str.toCharArray();
            Arrays.sort(charArray);
            String sortedStr = new String(charArray);

            if (!anagramMap.containsKey(sortedStr)) {
                anagramMap.put(sortedStr, new ArrayList<>());
            }
            anagramMap.get(sortedStr).add(str);
        }

        return new ArrayList<>(anagramMap.values());
    }

    public static void main(String[] args) {
        String[] strs = {"eat", "tea", "tan", "ate", "nat", "bat"};
        AnagramGrouping anagramGrouping = new AnagramGrouping();
        List<List<String>> result = anagramGrouping.groupAnagrams(strs);
        System.out.println(result);
    }
}
1. Initialization:

Create an empty HashMap to store anagrams. The key will be a sorted string, and the value will be a list of anagrams.
2. Iteration over Strings:

For each string in the input array:
Convert the string to a character array.
Sort the character array in ascending order.
Convert the sorted character array back to a string.
3. Anagram Grouping:

Check if the sorted string already exists as a key in the HashMap:
If it exists, add the current string to the corresponding list of anagrams.
If it doesn't exist, create a new entry in the HashMap with the sorted string as the key and a new list containing the current string as the value.   
4. Result Collection:

Convert the values of the HashMap (lists of anagrams) into a list of lists and return it.